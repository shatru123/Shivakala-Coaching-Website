@model (string Title, string Description)

<div class="section-header text-center mx-auto reveal">
    <span class="eyebrow">@T["SectionEyebrow"]</span>
    <h2>@Model.Title</h2>
    <p>@Model.Description</p>
</div>
